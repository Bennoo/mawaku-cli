{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cargo build",
            "type": "shell",
            "command": "cd mawaku-rs && cargo build",
            "group": "build"
        }
    ]
}